replicaCount: 1

image:
  repository: mishazyat123/misha-test:1.0
  pullPolicy: IfNotPresent
  tag: "latest"

autoscaling:
  enabled: false

service:
  type: ClusterIP
  port: 80

serviceAccount:
  create: true
  name: ""

ingress:
  enabled: true
  annotations: {}
  hosts:
    - host: hello-misha.yourdomain.com
      paths:
        - path: /
          pathType: ImplementationSpecific

